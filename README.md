Note: when you make a change to the helper tool, you must increment the version
in "Bundle Version" field of "Info.plist".  If you don't do that, `SMJobBless`
will not install the updated helper binary.
